# =============================================================================
# Main Release
# =============================================================================
# Triggered on: push to main, PR merged to main, or manual trigger
# =============================================================================
name: "🚀 Release — Main"

on:
  push:
    branches:
      - main
  pull_request:
    types: [closed]
    branches:
      - main
  workflow_dispatch:

permissions: # needed for semantic release and publishing releases
  contents: write
  id-token: write

concurrency:
  cancel-in-progress: false
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  quality-checks:
    name: "Quality Checks"
    uses: ./.github/workflows/quality-and-tests.yml
    secrets: inherit  # pragma: allowlist secret

  release:
    name: "🏷️ Stable Version & Tag"
    runs-on: ubuntu-latest
    needs: quality-checks
    timeout-minutes: 5
    outputs:
      released: ${{ steps.semantic_release.outputs.released }}
      version: ${{ steps.semantic_release.outputs.version }}
    steps:
      - name: Check Out
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Python Dependencies
        uses: ./.github/actions/install-python-deps

      - name: Configure Git Bot Identity
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Semantic Release — Bump Version & Tag
        id: semantic_release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          # Disable git pre-commit hooks
          git config core.hooksPath /dev/null
          echo "::group::Semantic Release output"
          NEW_VERSION=$(uv run semantic-release version --print | tr -d '[:space:]')
          if [ -n "$NEW_VERSION" ]; then
            uv run semantic-release version
            {
              echo "released=true"
              echo "version=$NEW_VERSION"
            } >> "$GITHUB_OUTPUT"
          else
            echo "released=false" >> "$GITHUB_OUTPUT"
            echo "No releasable commits found — skipping release."
          fi
          echo "::endgroup::"

      - name: Unset Git Hooks Path
        if: always()
        shell: bash
        run: |
          # Re-enable git pre-commit hooks
          git config --unset-all core.hooksPath || true

      - name: Cleanup Artifacts
        if: always()
        uses: ./.github/actions/cleanup-artifacts

  build-and-publish:
    name: "📦 Build & Publish (Stable)"
    runs-on: ubuntu-latest
    needs: release
    timeout-minutes: 15
    if: needs.release.outputs.released == 'true'
    steps:
      - name: Check Out
        uses: actions/checkout@v6
        with:
          fetch-depth: 0


      - name: Install Python Dependencies
        uses: ./.github/actions/install-python-deps

      - name: Build Package
        id: build
        shell: bash
        run: |
          echo "::group::Build output"
          make build-package
          echo "::endgroup::"

      - name: Publish to GitHub Release
        id: publish_gh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_ENTERPRISE_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          VERSION="${{ needs.release.outputs.version }}"
          if [ -z "$VERSION" ]; then
            echo "ERROR: No version from semantic release"
            exit 1
          fi
          export GH_HOST=$(echo "$GITHUB_SERVER_URL" | sed 's|https://||')
          if gh release view "v${VERSION}" &>/dev/null; then
            gh release upload "v${VERSION}" dist/*.whl dist/*.tar.gz --clobber
          else
            PREV_TAG=$(git tag --sort=-version:refname | grep -E '^v[0-9]' | sed -n '1p')
            CHANGELOG=$(git log "${PREV_TAG}"..HEAD --oneline 2>/dev/null | head -20 || echo "Release $VERSION")
            gh release create "v${VERSION}" dist/*.whl dist/*.tar.gz \
              --title "v${VERSION}" \
              --notes "## Changelog

              $CHANGELOG"
          fi

      - name: Cleanup Artifacts
        if: always()
        uses: ./.github/actions/cleanup-artifacts

  docs:
    name: "📚 Deploy Docs"
    runs-on: ubuntu-latest
    needs: [release, build-and-publish]
    timeout-minutes: 5
    if: needs.build-and-publish.result == 'success'
    steps:
      - name: Check Out
        uses: actions/checkout@v6
        with:
          fetch-depth: 0


      - name: Install Python Dependencies
        uses: ./.github/actions/install-python-deps

      - name: Deploy Docs to GitHub Pages
        id: deploy_docs
        shell: bash
        run: |
          echo "::group::Deploy Docs output"
          make deploy-doc-gh
          echo "::endgroup::"

      - name: Cleanup Artifacts
        if: always()
        uses: ./.github/actions/cleanup-artifacts

  summary:
    name: "📋 Summary"
    runs-on: ubuntu-latest
    needs: [quality-checks, release, build-and-publish, docs]
    if: always()
    timeout-minutes: 5
    steps:
      - name: Generate Summary
        shell: bash
        run: |
          RELEASED="${{ needs.release.outputs.released }}"
          VERSION="${{ needs.release.outputs.version }}"
          QC="${{ needs.quality-checks.result }}"
          REL="${{ needs.release.result }}"
          BUILD_PUB="${{ needs.build-and-publish.result }}"
          DOCS="${{ needs.docs.result }}"

          status_icon() { if [ "$1" == "success" ]; then echo "✅"; elif [ "$1" == "skipped" ]; then echo "⏭️"; else echo "❌"; fi; }

          echo "## 🚀 Main Release Pipeline" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "$BUILD_PUB" == "success" ] && [ "$DOCS" == "success" ]; then
            echo "> 🎉 **Release \`v$VERSION\` completed successfully**" >> $GITHUB_STEP_SUMMARY
          elif [ "$BUILD_PUB" == "skipped" ]; then
            echo "> ℹ️ **No releasable commits** — nothing published." >> $GITHUB_STEP_SUMMARY
          else
            echo "> ⚠️ **Release completed with issues** — check logs above." >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Step | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🧹 Quality Checks | $(status_icon $QC) |" >> $GITHUB_STEP_SUMMARY
          echo "| 🏷️ Semantic Release | $(status_icon $REL) |" >> $GITHUB_STEP_SUMMARY
          echo "| 📦 Build & Publish | $(status_icon $BUILD_PUB) |" >> $GITHUB_STEP_SUMMARY
          echo "| 📚 Deploy Docs | $(status_icon $DOCS) |" >> $GITHUB_STEP_SUMMARY
